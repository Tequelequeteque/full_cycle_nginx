server {
    listen ${NGINX_PORT};
    server_name fullcycle.localhost;

    location / {
        proxy_set_header   X-Forwarded-For $remote_addr;
        proxy_set_header   Host $http_host;
        proxy_pass         http://app:${APP_PORT};
    }
}